{
  "name": "updater",
  "version": "1.0.0",
  "description": "i am creating this because i use two pc's and i often forget to push code to github to use in the other pc. this tool allow to push every repository that has changes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "simple-git": "^3.33.0"
  }
}
